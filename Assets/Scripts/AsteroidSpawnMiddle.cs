using UnityEngine;

/// <summary>
/// Unused leftover. Medium asteroids must be created only when a large one splits.
/// Kept disabled in the scene; Update no longer floods the pool if re-enabled.
/// </summary>
public class AsteroidSpawnMiddle : MonoBehaviour
{
}
